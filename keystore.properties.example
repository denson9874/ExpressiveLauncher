# Copy this file to keystore.properties. The destination and all keystores are ignored by Git.
# Use an absolute path when the keystore is stored outside this checkout.
storeFile=/absolute/path/to/expressive-upload.jks
storePassword=replace-with-a-secret
keyAlias=expressive-upload
keyPassword=replace-with-a-secret
